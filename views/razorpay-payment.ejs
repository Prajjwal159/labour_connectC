<!DOCTYPE html>
<html lang="<%= currentLang %>">
<head>
    <meta charset="UTF-8">
    <title>Razorpay Payment</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="page-shell">
<div class="container page-content">
    <div class="row justify-content-center align-items-center" style="min-height:90vh;">
        <div class="col-lg-6">
            <div class="frost-card p-5 text-center">
                <h2 class="text-success fw-bold mb-3"><%= title %></h2>
                <p><strong>Amount:</strong> ₹<%= amount %></p>

                <button id="payBtn" class="btn pretty-btn-green mt-3">
                    Pay with Razorpay
                </button>

                <a href="<%= cancelLink %>?lang=<%= currentLang %>" class="btn pretty-btn-outline mt-3">
                    Cancel
                </a>
            </div>
        </div>
    </div>
</div>

<script src="https://checkout.razorpay.com/v1/checkout.js"></script>
<script>
document.getElementById("payBtn").onclick = function () {
    const options = {
        key: "<%= keyId %>",
        amount: "<%= order.amount %>",
        currency: "INR",
        name: "Namma Raitha Jobs",
        description: "<%= title %>",
        order_id: "<%= order.id %>",
        handler: async function (response) {
            const res = await fetch("/razorpay/verify", {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({
                    paymentSessionId: "<%= paymentSessionId %>",
                    razorpay_order_id: response.razorpay_order_id,
                    razorpay_payment_id: response.razorpay_payment_id,
                    razorpay_signature: response.razorpay_signature
                })
            });

            const html = await res.text();
            document.open();
            document.write(html);
            document.close();
        },
        prefill: {
            name: "<%= customerName || '' %>",
            email: "<%= customerEmail || '' %>",
            contact: "<%= customerPhone || '' %>"
        },
        theme: {
            color: "#198754"
        }
    };

    const rzp = new Razorpay(options);
    rzp.open();
};
</script>
</body>
</html>